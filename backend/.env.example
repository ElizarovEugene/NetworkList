# Copy to backend/.env and fill in before the first run (especially on a
# production machine) — without this, the defaults baked into the code apply.
jwt_secret=change-me-generate-with-secrets-token-hex-32
db_encryption_key=change-me-generate-with-secrets-token-hex-32
admin_username=admin
admin_password=admin
admin_language=en
